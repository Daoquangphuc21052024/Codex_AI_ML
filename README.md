# Codex_AI_ML

Repository for Codex machine learning tasks.

## Structure

All projects must live inside:

projects/<project_name>/

Agents must follow these rules:

- Never create files in repository root
- Work only inside project folders
- Each project has its own data, models, and source code
